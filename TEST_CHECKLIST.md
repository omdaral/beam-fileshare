# Beam — قائمة الاختبار الإلزامي (TEST CHECKLIST)
> المرجع: `docs/04-البنية-التحتية-Architecture.md` قسم 7 + مقاييس النجاح في `docs/01-الأهداف-Goals.md`.
> قابلة للطباعة: انسخها ورقياً وعلّم ✅ / ❌ لكل بند. البيئة: LAN أو Hotspot، البورت الافتراضي `2004`.

## 0) تجهيز عام (قبل كل البنود)
- [ ] السيرفر يعمل: `./Beam --port 2004` (أو `go -C goserver run . --port 2004`) ويطبع روابط `http://IP:2004`
- [ ] لحظة التشغيل: يُطبع **رابط دخول الأجهزة** + يظهر إشعار نظام به (لينكس)
- [ ] صفحة Beam على جهاز التشغيل: هيرو الدخول بخط كبير أعلى الصفحة + زر نسخ يعمل + زر ⚙️ الإعدادات ظاهر في الهيدر
- [ ] زر ⚙️ يفتح نافذة إعدادات بتبويبات (الشبكة/عام/الأجهزة والسجل/الجهاز) + تُغلق بـ Esc والضغط خارجها
- [ ] الدخول مباشر بدون كود (الأمان = باسورد الواي فاي فقط)
- [ ] مجلد `~/Downloads/Beam` يُنشأ تلقائياً، والسجل يُعرض من صفحة الويب (تبويب الأجهزة والسجل — ذاكرة فقط، لا ملفات)
- [ ] لحظة التشغيل: المتصفح يفتح تلقائياً على الواجهة + زر ⏻ «إيقاف السيرفر» دائري ثابت الحجم في الهيدر (للمالك، ضغطة واحدة توقف وتقفل التاب)
- [ ] بعد 5 ساعات خمول (أو `--idle-timeout 1m` للتجربة) يُغلق السيرفر نفسه
- [ ] اختبارات Go خضراء: `go -C goserver test ./...`
- [ ] النشر بضغطة: `./publish.sh` ينتهي بـ «تم النشر بنجاح» والسيرفر الجديد يرد بهوية Beam (راجع `/tmp/beam-server.log` عند الفشل)

## 0-ب) صفحة واحدة + منطقة مالك مشروطة (إلزامي)
```bash
BASE=http://127.0.0.1:2004
# نفس الصفحة للجميع على كل العناوين:
for p in / /index.html /admin /guest; do curl -s -o /dev/null -w "$p:%{http_code} " $BASE$p; done; echo
curl -s $BASE/ | grep -c "ownerZone\|Beam"
# متوقع: 200 200 200 200 ثم عدد > 0
# من موبايل على الشبكة (ضيف):
# - نفس الصفحة تفتح (هيرو + ملفات + QR) بدون زر ⚙️ ولا نافذة الإعدادات — وأي API إداري يرد 403
# - المالك لو فتح بعنوان LAN يرى نسخة الضيف (بدون ⚙️) — الحل: التبويب المفتوح تلقائياً على جهازه
# - زر مسح السجل (تبويب الأجهزة والسجل) يفرّغ السجل من الذاكرة بالكامل (بتأكيد ضغطتين)
# - قائمة الأجهزة المتصلة الآن تتحدث كل 3 ثوانٍ في نافذة المالك
# - اللغة الافتراضية: غيّرها من تبويب "عام" واحفظ → زائر جديد (متصفح نظيف) يفتح بها، والضيف يبدل لغته من الهيدر بحرية
# - المجلدات: ارفع مجلدًا (زر + سحب وإفلات) → شجرة قابلة للطي → بحث فوري → تنزيل zip وفكّه سليم → حذف ملف مفرد → حذف مجلد كامل (ضغطتين) → تقليم الفارغ
# - سقف الشرائط: ارفع مجلدًا كبيرًا (50+ ملف) → صف مجمّع واحد بعدّاد وشريط إجمالي + ≤5 شرائط فردية + المنتهي يختفي ويتبقى آخر 5 → ألغِ دفعة mid-flight وتأكد التوقف
# - الحزم: deb يُبنى وlintian نظيف (عدا هوية الصيانة) + AppImage يُبنى ويعمل (health) + manifest/metainfo متحقق منها — بوابة قبول
# - مجلد مضغوط دائمًا: ارفع مجلدًا → جلسة zip واحدة → الشجرة تظهر بعد الاكتمال → ملف zip مفرد يبقى كما هو بلا فك
# - الأوضاع: ارفع نفس الملف موثوق ثم تيربو (شارة تيربو ظاهرة) → اقطع mid-transfer: الموثوق يكمل من القطعة، والتيربو يعيد القطع الكبيرة فقط → نزّل بالوضعين وقارن
# - مجلد متجاوز للسقف: يظهر كصف zip مع السبب ولا يُفتح كشجرة — بوابة قبول
# - مسح QR الواي فاي وQR الرابط بالموبايل: اتصال/فتح فوري — بوابة قبول
# - الهوية: شعار Beam ظاهر + الثيم فاتح ورقي افتراضياً + مفتاح يبدل للداكن ويحفظ الاختيار
# - زر اللغة EN/عربي يقلب الاتجاه والنصوص كاملة (تحقق من شاشة الملفات والأزرار)
# - QR داخل إطار أبيض حتى في الداكن + الخط عربي سليم بدون إنترنت
```
- [ ] الضيف لا يرى أي زر إدارة في HTML الصفحة — النتيجة: ✅ / ❌

### أوامر الدخان الأساسية (Smoke) — تعمل على أي جهاز في نفس الشبكة
```bash
BASE=http://127.0.0.1:2004   # غيّر IP حسب المطبوع، و PORT حسب --port

# 1) الصحة
curl -s $BASE/health
# متوقع: {"ok": true}

# 2) قائمة الملفات (مباشرة بدون دخول)
curl -s $BASE/files
# متوقع: {"files": [...]}

# 3) رفع ملف تجريبي (بروتوكول مقسم v1 — بالأدوات القياسية فقط)
echo "smoke-test" > /tmp/smoke.txt
UUID=$(openssl rand -hex 16)
NAME=smoke.txt; SIZE=$(stat -c%s /tmp/smoke.txt)
curl -s -H 'Content-Type: application/json' -d "{\"uuid\":\"$UUID\",\"name\":\"$NAME\",\"size\":$SIZE}" $BASE/upload_init
curl -s --data-binary @/tmp/smoke.txt "$BASE/upload_chunk?id=$UUID&offset=0"
curl -s -H 'Content-Type: application/json' -d "{\"id\":\"$UUID\"}" $BASE/upload_complete
# متوقع: complete: 200 {'ok': true, 'saved': [...]}
# ملاحظة: مسار التوافق POST /upload (multipart حتى 128MB) ما زال يعمل للأدوات القديمة:
# curl -s -F "files=@/tmp/smoke.txt" $BASE/upload
# متوقع: {"ok": true, "saved": [...]}

# 4) تنزيله ومقارنته
curl -s "$BASE/download?file=smoke.txt" -o /tmp/smoke_dl.txt
diff /tmp/smoke.txt /tmp/smoke_dl.txt && echo SAME || echo DIFFERENT
# متوقع: SAME

# 5) حذف + تأكيد اختفائه
curl -s -X POST -H 'Content-Type: application/json' -d '{"file":"smoke.txt"}' $BASE/delete
curl -s "$BASE/download?file=smoke.txt"
# متوقع: {"ok": true} ثم {"error": "الملف غير موجود"}

# دالة رفع مجزأ للاختبار (v1: init/chunk/complete بقطع 4MB)
# الاستخدام: bigup $BASE FILE [UUID] [STOP_AFTER_BYTES]
bigup() {
  local BASE=$1 FILE=$2 UUID=${3:-$(openssl rand -hex 16)} STOP=${4:-}
  local NAME=$(basename "$FILE") SIZE=$(stat -c%s "$FILE")
  curl -s -H 'Content-Type: application/json' \
    -d "{\"uuid\":\"$UUID\",\"name\":\"$NAME\",\"size\":$SIZE}" $BASE/upload_init > /dev/null
  local OFF=0 SENT=0 CH=4194304
  while [ "$OFF" -lt "$SIZE" ]; do
    tail -c +$((OFF+1)) "$FILE" 2>/dev/null | head -c $CH | \
      curl -s --data-binary @- "$BASE/upload_chunk?id=$UUID&offset=$OFF" > /tmp/bigup_resp.json
    OFF=$(grep -o '"offset":[0-9]*' /tmp/bigup_resp.json | grep -o '[0-9]*' | tail -1)
    [ -z "$OFF" ] && { echo "UPLOAD FAIL"; cat /tmp/bigup_resp.json; return 1; }
    SENT=$OFF
    echo "offset: $OFF"
    if [ -n "$STOP" ] && [ "$SENT" -ge "$STOP" ]; then echo "SIMULATED-DROP at $OFF"; return 0; fi
  done
  curl -s -H 'Content-Type: application/json' -d "{\"id\":\"$UUID\"}" $BASE/upload_complete
}

# 6) رفع متزامن لنفس الاسم (تضارب) — يجب ملفان لا فقدان
echo a > /tmp/t1.txt; echo b > /tmp/t2.txt
mkdir -p /tmp/r1 /tmp/r2; cp /tmp/t1.txt /tmp/r1/race.txt; cp /tmp/t2.txt /tmp/r2/race.txt
U1=$(openssl rand -hex 16); U2=$(openssl rand -hex 16)
( bigup $BASE /tmp/r1/race.txt $U1 > /dev/null 2>&1; echo " <- race1 done" ) &
( bigup $BASE /tmp/r2/race.txt $U2 > /dev/null 2>&1; echo " <- race2 done" ) &
wait
curl -s $BASE/files | grep -o "race[^\"]*"
# متوقع: race.txt و race (1).txt — لا ملفات .part ظاهرة
```

---

## 1) دبل كليك على جهاز نظيف (بدون Python/Go)
> مقياس النجاح: من الدبل كليك لظهور الشبكة/الرابط < 30 ثانية. تثبيت مكتبات على العميل = صفر.
- [ ] انسخ المجلد كله (الأيقونة `Beam.desktop` + البرنامج `Beam`) إلى جهاز نظيف بدون Python/Go
- [ ] دبل كليك → تظهر سطور `Company Share شغال` + روابط IP:Port خلال < 30 ثانية
- [ ] افتح `http://127.0.0.1:2004/health` من نفس الجهاز
```bash
curl -s http://127.0.0.1:2004/health
# متوقع: {"ok": true}
```
- [ ] النتيجة: ✅ / ❌ — الزمن المقاس: ____ ثانية — ملاحظات: ____

## 2) أندرويد + آيفون: رفع وتنزيل
> مقياس النجاح: عميل جديد من دخول الواي فاي لأول تحميل < دقيقة.
- [ ] الأندرويد: دخل على `http://IP:2004` مباشرة (بدون كود) → رفع صورة → نزّلها → opened OK
- [ ] الآيفون (Safari): نفس الخطوات → رفع + تنزيل OK
```bash
# من لابتوب مساعد على نفس الشبكة (محاكاة عميل موبايل):
BASE=http://192.168.1.5:2004   # غيّر IP
echo hi > /tmp/m.txt && bigup $BASE /tmp/m.txt
curl -s "$BASE/download?file=m.txt" -o /tmp/m_dl.txt && diff /tmp/m.txt /tmp/m_dl.txt && echo SAME
```
(الصق دالة `bigup` من قسم الدخان أعلاه أولاً.)
- [ ] النتيجة: ✅ / ❌ — ملاحظات: ____

## 3) ملف كبير ذهاب وعودة + فصل واستكمال (500MB)
> مقياس النجاح: نقل كبير بدون تحميل الرام + الاستكمال من مكان التوقف + عداد سرعة ووقت متبقٍ.
> بروتوكول القطع v2: قطع 2MB متوازية (×3) + بصمة SHA-256 لكل قطعة + تحقق ذاتي (التالف يُعاد وحده).
- [ ] ولّد ملف 500MB وارفعه، افصل mid-upload (أو استخدم STOP_AFTER)، أعد الرفع بنفس UUID → يكمل → نزّل وقارن SHA256
```bash
BASE=http://127.0.0.1:2004
head -c 524282004 /dev/urandom > /tmp/big500.bin
sha256sum /tmp/big500.bin | tee /tmp/big.sha
UUID=$(openssl rand -hex 16)
bigup $BASE /tmp/big500.bin $UUID 100000000   # رفع 100MB ثم فصل مُحاكى
bigup $BASE /tmp/big500.bin $UUID             # استكمال من مكان التوقف → complete
curl -s "$BASE/download?file=big500.bin" -o /tmp/big500_dl.bin
sha256sum -c <(sed 's|/tmp/big500.bin|/tmp/big500_dl.bin|' /tmp/big.sha) && echo ROUNDTRIP_OK
# متوقع: ROUNDTRIP_OK + لا ملفات .part في القائمة
```
- [ ] من المتصفح: أعد اختيار نفس الملف بعد تحديث الصفحة mid-upload → يكمل (find تلقائي) ولا يبدأ من الصفر
- [ ] من متصفح آخر/جهاز آخر: اختر نفس الملف → يُتبنّى التقدم الموجود (استكمال بهوية جديدة)
- [ ] عداد السرعة والوقت المتبقي ظهر أثناء الرفع؟ نعم / لا — السرعة المقاسة: ____
- [ ] زر ⚡ سريع في التنزيل: تقدم + سرعة + إيقاف/استئناف + تحقق بصمة؟ نعم / لا
- [ ] سطر السجل يحتوي المدة والسرعة؟ (السجل من صفحة الويب — تبويب الأجهزة والسجل — مثال: `file (123b, 1.2s, 3.4 م.ب/ث)`)
- [ ] النتيجة: ✅ / ❌ — زمن الرفع: ____ — زمن التنزيل: ____

## 4) فصل الواي فاي أثناء الرفع → رسالة واضحة
> متوقع: رسالة عربية واضحة في الواجهة (`انقطع الاتصال... حاول تاني`) + لا ملف ناقص يُعرض كاملاً.
- [ ] ابدأ رفع ملف كبير (100MB) ثم افصل واي فاي العميل mid-upload
- [ ] تأكد الرسالة في المتصفح واضحة (لقطة شاشة مرفقة؟)
- [ ] أعد الاتصال وأعد الرفع → ينجح
```bash
# بعد إعادة الاتصال — تحقق سلامة القائمة وعدم وجود ملف فاسد:
BASE=http://127.0.0.1:2004
curl -s $BASE/files
# بعد إعادة الاتصال — تحقق سلامة القائمة (والسجل من صفحة الويب):
# متوقع: قائمة JSON سليمة + سطر upload واحد مكتمل فقط للملف المعاد رفعه
```
- [ ] النتيجة: ✅ / ❌ — نص الرسالة الظاهرة: ____

## 5) إيقاف/تشغيل 5 مرات بدون ريستارت للجهاز
> متوقع: كل مرة يشتغل على نفس البورت ويطبع نفس الروابط، بدون `Address already in use` عالق.
- [ ] كرر 5 مرات: `Ctrl+C` ثم `./Beam --port 2004` (أو إغلاق/فتح الـ exe)
```bash
for i in 1 2 3 4 5; do
  echo "=== دورة $i ==="
  ./Beam --port 2004 & SRV=$!
  sleep 3
  curl -s http://127.0.0.1:2004/health || echo "FAIL cycle $i"
  kill $SRV 2>/dev/null; wait $SRV 2>/dev/null
  sleep 1
done
# متوقع: {"ok": true} خمس مرات، بدون FAIL
```
- [ ] الدورات الناجحة: __ / 5 — النتيجة: ✅ / ❌

## 6) 8 أجهزة متزامنة بدون تهنيج
> مقياس النجاح: 8 أجهزة ترفع/تنزّل معاً بدون تهنيج.
- [ ] وصّل 8 عملاء (موبايلات + لابتوبات) على نفس الشبكة في نفس الوقت
- [ ] كل جهاز: فتح الرابط مباشرة + رفع ملف صغير + تنزيل ملف + `/files`
```bash
# محاكاة 8 عملاء متزامنين من جهاز واحد:
BASE=http://127.0.0.1:2004
for i in 1 2 3 4 5 6 7 8; do
  ( echo "client$i-data" > /tmp/c$i.txt \
    && bigup $BASE /tmp/c$i.txt $(openssl rand -hex 16) > /dev/null 2>&1 \
    && curl -s $BASE/files | head -c 200; echo " <- client$i done" ) &
done; wait
# متوقع: 8 أسطر "done" بدون أخطاء، والسيرفر ما زال يرد على /health
curl -s $BASE/health
```
- [ ] الأجهزة الناجحة: __ / 8 — النتيجة: ✅ / ❌ — ملاحظات (تهنيج/بطء): ____

---

## جدول التوقيع النهائي
| البند | ✅/❌ | الملاحظات |
|---|---|---|
| 1) جهاز نظيف |  |  |
| 2) أندرويد + آيفون |  |  |
| 3) 100MB ذهاب وعودة |  |  |
| 4) فصل أثناء الرفع |  |  |
| 5) إيقاف/تشغيل ×5 |  |  |
| 6) 8 أجهزة |  |  |

المختبر: ____ — التاريخ: ____ — النسخة (`VERSION`): ____ — نظام التشغيل: ____
