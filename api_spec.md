# مواصفة واجهات الحالة والإدارة

## `GET /api/status` (عام — يغذي كارت الشبكة وQR في الصفحتين)

| الحقل | النوع | الوصف |
|---|---|---|
| `running` | boolean | هل السيرفر شغال (`true` دائماً عملياً) |
| `ssid` | string | اسم شبكة الواي فاي، مثال `Beam` |
| `ip` / `port` / `url` | string/number/string | عنوان الدخول الكامل، مثال `http://192.168.1.5:2004` (البورت ثابت 2004) |
| `clients` | any | `—` أو قائمة IPs في وضع الهوتسبوت |
| `lan_mode` | boolean | `true` لو واي فاي `false` لو Hotspot مباشر |
| `security` | string | `wpa` أو `open` |
| `wifi_password` | string | **ظاهر للجميع عمداً** — الصفحة قناة مشاركة QR الواي فاي |
| `version` / `is_admin` | string/boolean | النسخة، وهل الطالب صاحب الجهاز (localhost) |
| `default_lang` | string | اللغة الافتراضية للزوار الجدد (`ar` أو `en`) — للجلسة فقط، والمتصفح يحفظ نسخة في localStorage |
| `shared_dir` | string | مسار مجلد المشاركة الوحيد (`~/Downloads/Beam`) — للعرض في الصفحة |
| `mdns_url` | string | `http://beam.local:2004` — اسم جميل يعمل على كل الأنظمة (mDNS) |
| `plain_url` | string | `http://beam:2004` — يعمل على ويندوز (LLMNR) |
| `idle_seconds` | number | ثوانٍ متبقية قبل الإغلاق التلقائي للخمول (`-1` لو معطّل) |

## الصفحات (لاندنج بيدج واحدة للجميع)
- `/` و`/index.html` و`/admin` و`/guest` تقدم نفس الصفحة الواحدة (توافق مع الروابط القديمة).
- سكاشن full-bleed: هيرو الدخول + خطوات + الشبكة/QR + الملفات + مساعدة — بلا كروت ضيقة.
- زر الإعدادات ⚙️ في الهيدر ونافذة `#ownerZone` (Modal بتبويبات: الشبكة/عام/الأجهزة والسجل/الجهاز) تظهر لصاحب الجهاز فقط — وكل API إداري يرفض غيره (`403`).
- زر اللغة في الهيدر للجميع (اختيار محلي)؛ وقائمة اللغة داخل الإعدادات تحدد `default_lang` للزوار الجدد بعد الحفظ.
- لا كود ولا كوكي ولا دخول عن بعد — أي طلب غير-localhost لأي إدارة ← `403`.

## endpoints الإدارة (صاحب الجهاز فقط)
| المسار | الوصف |
|---|---|
| `GET /api/net/status` | الحالة الكاملة (قائمة IPs للمالك، العدد للضيوف) |
| `GET /api/net/clients` | مسار خفيف للأجهزة اللحظية (يُستطلع كل 3 ثوانٍ): `{clients, clients_count}` |
| `POST /api/net/start` / `/api/net/stop` | تشغيل/إيقاف الشبكة (LAN/هوتسبوت) |
| `GET /api/logs` / `POST /api/logs/clear` | عرض السجل (حلقة ذاكرة: آخر 500 سطر) / **تفريغه بالكامل** |
| `GET`+`POST /api/config` | الإعدادات **للجلسة فقط — لا ملف config.json** (بدون أي حقل كود — أُزيل نهائياً). يقبل `port` و`max_file_mb` و`ssid` و`default_lang` (`ar`/`en` فقط — غيرها `400`). الواجهة تحفظ نسخة في localStorage وتعيد تعبئتها تلقائياً |
| `POST /api/server/stop` | إيقاف السيرفر (يخرج العملية) — له زر دائري ⏻ بأيقونة فقط في هيدر الصفحة (للمالك فقط، ضغطة واحدة ثم يقفل التاب أو يعرض شاشة توقف) |

## المجلدات (v1.4.0)
- المسارات نسبية بأسلوب `Docs/2026/a.pdf` (عمق ≤ 10، طول ≤ 512، بلا نقطية/روابط رمزية) — المخالف `400`.
- `GET /files` → `{files:[{name,path,size,mtime}], dirs:[{path,files,over}], truncated}`.
  `over` فارغ (قابل للفتح) أو `files`/`depth`/`list`: المجلد المتجاوز **لا يُعرض كشجرة** — الواجهة تقدمه كصف zip مباشر مع السبب.
- `GET /download?file=a/b.txt` و`GET /file_hash?file=a/b.txt` يعملان بالمسارات (مع Range).
- `GET /download_zip?dir=Docs` يبث المجلد zip (فحص مسبق: فارغ `404`، فوق حد الضغط `413`).
- `POST /delete` يقبل `{file:path}` أو `{dir:path}` (مسح تكراري آمن داخل مجلد المشاركة + تقليم المجلدات الفارغة).
- بروتوكول الرفع المقسم يقبل `name` بمسار نسبي — كل ملف جلسة مستقلة (استكمال/بصمات كما هي).

## أوضاع النقل + مجلدات مضغوطة دائمًا (v1.5.0)
- المجلدات تُضغط في المتصفح أولًا **دائمًا** (Deflate إن توفر وإلا STORE) ثم تُرفع كملف `.zip` واحد قابل للاستكمال؛ ملف `.zip` مفرد يُحفظ كما هو ولا يُفك.
- `upload_init` يقبل `noverify:true` (جلسة تيربو: قطع بلا بصمات) و`extract:true` (نية الفك).
- `upload_complete` يقبل `full_hash` (إجباري لجلسات التيربو `422` بدونه) و`extract`؛ نية الفك لاصقة بالجلسة فتعمل مع الاستئناف.
- بعد الاكتمال: استخراج خلفي للشجرة (فحص مسبق + سقوف + حماية zip-slip + دمج بلا كتابة فوق) ثم حذف الـ zip؛ أي فشل يُبقي الـ zip مع سطر سجل فقط.
- الوضع **موثوق** (2MB ×3 + بصمة/قطعة، افتراضي) و**تيربو** (8MB ×6 + تحقق نهائي واحد) — مفتاح لكل عملية رفع/تنزيل، والتنزيل التيربو بلا حفظ IndexedDB.

## QR (مكتبة qrcode-generator المضمنة — لا CDN)
يُقدَّم `/vendor/qrcode.js` من الباينري. تبويبا QR الواي فاي (بالباسورد المضمّن) وQR الرابط يعملان على الصفحتين. بوابة القبول: مسح حقيقي بالموبايل.

## الواجهة المقسمة (بدون bundler — v1.6.x)
- `GET /` هيكل Shell خفيف + `/app/*.js` (نفس `go:embed`, بدون build step):
  `config` (الحدود) → `i18n` → `api` (نقل/SHA-256) → `ui` → `files` →
  `upload` + `upload_queue` + `upload_zip` → `download` → `net` → `app` (boot).
- `GET /api/status` يتضمن `limits` (القطع/الكبس/البحث/الـ polling) — الواجهة
  تطبقها عبر `applyLimits()` وتعيد ضبط المؤقتات (`resetPolling`) بدل نسخة صلبة.
- الأمان: `Content-Security-Policy` + `X-Frame-Options: SAMEORIGIN` + `X-Content-Type-Options: nosniff` على HTML والملفات.
- التحديث: tick موحد + إيقاف كامل عند `document.hidden` + `fetchClients` للمالك فقط والمودال مفتوح.

## الإعدادات المركزية (v1.6.x)
- كل الحدود والأحجام والمهلات في `goserver/limits.go` (مصدر واحد) وتقبل
  تجاوز `BEAM_*` (مثال: `BEAM_PORT`, `BEAM_PIECE_MAX`, `BEAM_IDLE_TIMEOUT=90s`,
  `BEAM_CHUNK_MAX_V2`, `BEAM_MULTIPART_MAX`) — القيم الفارغة/الغلط تتجاهل بأمان.

## الموبايل: PWA-lite + غلاف أصلي (v1.6.0)
- `GET /manifest.json` (ثابت مضمن) + `/vendor/icon-192.png` + `/vendor/icon-512.png` + `/vendor/apple-touch-icon.png` + وسوم `theme-color`/آبل في الصفحة — أيقونة Beam على شاشة الموبايل تفتح الواجهة بملء الشاشة (تثبيت يدوي من المتصفح).
- بلا Service Worker عمداً: أصل LAN (`http://IP:2004`) ليس سياقاً آمناً فيستحيل تسجيله — لا كود ميت.
- مجلد `mobile/` (Capacitor): صفحة إعداد خفيفة (اكتشاف/مسح الشبكة + حفظ الرابط) توجّه WebView للسيرفر — كل الـ APIs تبقى same-origin بلا تعديل. سلك الخلفية في الصفحة (`bgKick`/`bgIdleCheck`) no-op على الويب ويشغّل خدمة أمامية داخل التطبيق الأصلي فقط.

## بدون ملفات جانبية + إغلاق تلقائي (v1.6.0)
- لا `config.json` ولا مجلد `logs/` ولا `Shared/` بجانب البرنامج — نهائياً.
- البورت ثابت **2004** (`http://<ip>:2004`)، ويُغيَّر فقط بفلاج `--port`.
- مجلد المشاركة الوحيد: `~/Downloads/Beam` (يُنشأ تلقائياً + ترحيل لمرة واحدة من `Shared/` القديم).
- السيرفر يفتح المتصفح بنفسه عند التشغيل (إلا مع `--no-browser`).
- أي طلب HTTP (عدا `/health`) يصفّر عدّاد الخمول؛ بعد **5 ساعات** صمت (`--idle-timeout`, و`0` للتعطيل) يُغلق السيرفر نفسه.
