{
  "name": "beam-mobile",
  "version": "1.7.1",
  "private": true,
  "description": "Beam native wrapper (Capacitor) — reuses goserver/web 1:1, adds an Android foreground service so uploads survive the background.",
  "scripts": {
    "sync": "./sync-web.sh",
    "build:apk": "./build-apk.sh"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capawesome-team/capacitor-android-foreground-service": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
